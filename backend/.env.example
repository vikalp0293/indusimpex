PORT=4000
NODE_ENV=development

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_USER=root
DB_PASSWORD=
DB_NAME=indusimpex

# Auth
JWT_SECRET=change-me

# CORS — comma-separated list of allowed origins (admin + website)
CORS_ORIGINS=http://localhost:5173,http://localhost:3000
